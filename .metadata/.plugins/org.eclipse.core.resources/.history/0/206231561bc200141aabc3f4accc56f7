var tbl_data = 
[
	{title:'Row 1'}, 
	{title:'Row 2'}, 
	{title:'Row 3'},
];

var view = Titanium.UI.createView({
  backgroundColor:'red',
  width: 20, height: 20
});
var lable = Ti.UI.createLabel({
	
});
view.add(lable);

$.TV.add(view);


$.TV.setData(tbl_data);

$.index.open();
